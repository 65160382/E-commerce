<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order payment</title>
    <link rel="stylesheet" href="/css/style.css" />
    <link rel="stylesheet" href="/css/chart.css" />
    <link rel="icon" href="/img/bag.png" type="image/png">
    <script src="js/chart.js"></script>
</head>
<body>
    <%- include('partials/header') %>
    <%- include('partials/slidebar') %>

    <div class="container">
        <div class="breadcrumb" id="breadcrumb">
            <!-- หน้าแรก / รายละเอียดสินค้า / ตะกร้าสินค้า -->
        </div>
        <table>
            <thead>
                <tr>
                    <th>รูปสินค้า</th>
                    <th>สินค้า</th>
                    <th>ราคา/หน่วย</th>
                    <th>จำนวน</th>
                    <th>จำนวนเงินรวม</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                <% products.forEach(function(product) { %>
                <tr>
                    <td><img src="<%= product.image %>" alt="<%= product.name %>" class="product-image"></td>
                    <td><%= product.name %></td>
                    <td data-price="<%= product.price %>"><%= product.price.toFixed(2) %></td>
                    <td><input type="number" value="<%= product.quantity %>" class="quantity-input" data-id="<%= product.id %>" onchange="updateQuantity(this)"></td>
                    <td class="total-price"><%= (product.price * product.quantity).toFixed(2) %></td>
                    <td><button class="btn btn-danger" data-id="<%= product.id %>" onclick="deleteProduct(this)">ลบทิ้ง</button></td>
                </tr>
                <% }); %>
            </tbody>
        </table>

        <div class="total">
            จำนวนเงินรวมทั้งหมด <span id="totalPrice"><%= totalPrice.toFixed(2) %></span> บาท
        </div>
        
        <% products.forEach(function(product) { %>
            <div class="action-buttons">
                <form action="/order" method="post">
                    <input type="hidden" name="product_id" value="<%= product.id %>">
                    <input type="hidden" name="customer_id" value="<%= customer_id %>">
                    <input type="hidden" name="quantity" value="<%= product.quantity %>">
                    <button class="btn btn-success">สั่งซื้อสินค้า</button>
                </form>
            </div>
        <% }); %>
        
    </div>
</body>
</html>
